-- Note: This is a Pandas problem, not SQL.
-- import pandas as pd
-- def createDataframe(student_data):
--     return pd.DataFrame(student_data, columns=['student_id', 'age'])
SELECT 'This problem requires Pandas, not SQL' AS note;
